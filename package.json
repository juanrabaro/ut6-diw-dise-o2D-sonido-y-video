{
  "name": "proyecto",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "clean": "rimraf dist .cache .cache-loader .parcel-cache",
    
    "dev": "npm-run-all clean start",
    "buildClean": "npm-run-all clean build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  }
}